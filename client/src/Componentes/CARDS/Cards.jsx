import style from './cards.module.css'
import Card from '../CARD/Card';

const Cards = ({id, name, }) => {};  //{id, name, difficulty, duration, season, country, handleDelete, img}

export default Cards;